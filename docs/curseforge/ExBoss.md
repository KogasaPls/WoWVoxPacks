---
summary: English voice callouts for ExBoss in the {Voice} voice.
---
# ExBoss: WoWVoxPacks ({Voice})

A TTS voice pack for ExBoss. Its Chinese callout labels are spoken in English by the Google Cloud **{Voice}** voice ({VoiceDescription}), so a cue labeled 准备接圈 says "Soak".

## Turn it on

ExBoss lists the pack as a voice pack of its own. Pick **ExBoss WoWVoxPacks {Voice}** in its voice settings and every callout it speaks comes from these recordings. The entries keep ExBoss's own Chinese labels, because that is how ExBoss looks a callout up; only the audio is English.

## Requirements

ExBoss, and LibSharedMedia-3.0, which comes bundled with almost every raid addon.

{PacksTable}

## Voices

Wavenet_E (female), Neural2_C (female) and Studio_Q (male). Samples are on the [GitHub page](https://github.com/KogasaPls/WoWVoxPacks#voices). Install as many of them as you like; they sit side by side.

## Source

Generated from Google Cloud Text-to-Speech by [WoWVoxPacks](https://github.com/KogasaPls/WoWVoxPacks), Apache-2.0. A wrong or missing label is worth an issue there.
